<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DigitalScoreboard"
             x:Class="DigitalScoreboard.RefereePage"
             x:DataType="local:RefereeViewModel"
             Title="Referee">

    <ScrollView>
        <VerticalStackLayout>
            <!--<RadioButton IsChecked="{Binding HomePosession}"
                         Value="1"
                         Content="Home"
                         GroupName="Posession" />

            <RadioButton IsChecked="{Binding HomePosession}"
                         Value="2"
                         Content="Away"
                         GroupName="Posession" />-->

            <Button Text="Next Down" Command="{Binding IncrementDown}" />
            <Button Text="Next Quarter" Command="{Binding IncrementPeriod}" />
            <Button Text="Toggle Game Clock" Command="{Binding TogglePeriodClock}" />
            <Button Text="Start Play Clock" Command="{Binding TogglePlayClock}" />

            <Button Text="Set Home Score" />
            <Button Text="Set Away Score" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
